model_options:
  test_size: 0.2
  random_state: 3
  features:
    - engines
    - passenger_capacity
    - crew
    - d_check_complete
    # - moon_clearance_complete  # constant in raw data, causes singular matrix
    - iata_approved
    - company_rating
    - review_scores_rating
